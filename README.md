# represent a list as a chain of nodes

**recursive toString()**
- *Note* : the recursive iteration occurs in a helper function. The toString() itself only encapsulates the String produced by this helper function in brackets. 
